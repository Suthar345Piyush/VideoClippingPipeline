version: "2"
sql:
  - engine: "sqlite"
    schema: "database/migrations"
    queries: "sql/queries.sql"
    gen:
      go:
        package: "sql"
        out: "sql"

        emit_json_tags: true
        emit_interface: true